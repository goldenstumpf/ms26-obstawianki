# Technical

DEMO_MODE uses demo data in `demo_db/matches.json`; for UI crest rendering tests, set every match's `home_crest` and `away_crest` fields to local static URLs `/app/static/crests/home.svg` and `/app/static/crests/away.svg`.

**Context:** User asked to prepare JSON and demo mode so they can test crest rendering in the UI; assistant de...
**Why it matters:** Enables consistent, offline UI testing in DEMO_MODE by ensuring deterministic crest assets across...