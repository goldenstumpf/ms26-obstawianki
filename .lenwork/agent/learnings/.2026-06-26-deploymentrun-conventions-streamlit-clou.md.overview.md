# Technical

Deployment/run conventions: Streamlit Cloud entrypoint is streamlit_app.py at repo root calling app.main.run; Railway worker runs via python -m app.worker.worker.

**Context:** Review of repo deploy targets and README run commands.
**Why it matters:** Helps maintain consistent packaging/imports and correct deployment documentation going forward.