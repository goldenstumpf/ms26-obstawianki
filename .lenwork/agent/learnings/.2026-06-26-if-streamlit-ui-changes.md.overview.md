# Workflow

If Streamlit UI changes involving static assets don’t appear, restart Streamlit (stop/re-run) because it may keep files loaded/cached.

**Context:** Post-change verification instructions after updating crest rendering for local SVGs.
**Why it matters:** Saves time troubleshooting when UI doesn’t reflect code/static asset changes during development.