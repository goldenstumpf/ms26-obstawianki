# Context-dev-setup Conventions

**Updated:** 2026-06-25 15:31 UTC

## Context / Dev Setup

- Local Streamlit app `ms26-obstawianki` run on Windows (PowerShell, Python 3.12 venv) hits Supabase via postgrest/httpx during login and crashes on corporate laptop with TLS interception: `httpx.ConnectError: [SSL: CERTIFICATE_VERIFY_FAILED] unable to get local issuer certificate`.
