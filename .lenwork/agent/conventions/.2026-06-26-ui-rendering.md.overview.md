# UI Rendering

- In `app/utils/components.py` (Streamlit components), render bet in view/report mode as two lines: main bet score on first line; bet DIP on second line styled as same color as bet, slightly smaller ...