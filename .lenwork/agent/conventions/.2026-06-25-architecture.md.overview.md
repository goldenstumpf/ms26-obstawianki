# Architecture

- Project has legacy Supabase-calling layer under `app/core/*` (e.g., `app/core/bets.py:get_full_bets_info()` using postgrest/httpx) and a newer demo-safe DAL under `app/data/*` gated by `DEMO_MODE`....