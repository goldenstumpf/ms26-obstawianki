# Encoding/Localization

- Keep `app/utils/components.py` saved as UTF-8 to preserve emoji literals (🔴/🟡/⚫, 🏆, ✔, ✏️). If emojis appear as '??' or imports fail with UTF-8 errors, check for accidental cp1250/...