# Domain Rules

- Extra time possible condition: treat as knockout stage; implemented via `_is_knockout_stage(r)` (DIP present / not group stage). Only then show `w 90 minut` for regular-time finishes and append out...