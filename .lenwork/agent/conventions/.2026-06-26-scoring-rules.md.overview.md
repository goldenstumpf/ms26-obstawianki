# Scoring & Rules

- Scoring v2 requirements: base points 4 exact score, 2 goal diff, 1 result (home/away/draw; draw result counts only in GROUP_STAGE). Near-miss bonus +0.5 if abs(home_bet-home_goals)+abs(away_bet-awa...