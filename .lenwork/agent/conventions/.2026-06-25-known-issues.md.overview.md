# Known Issues

- app/services/monitor.py infinite loop monitors matches needing monitoring; helper has_matches_to_monitor() references supabase without defining it (likely missing supabase = get_supabase()), may er...