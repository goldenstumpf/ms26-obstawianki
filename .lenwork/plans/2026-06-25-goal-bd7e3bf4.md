---
plan_id: plan-bd7e3bf4
created: 2026-06-25T14:47:59.877427200+00:00
request: "Try again to remove the DEMO MODE from this project"
status: approved
---

Plan to remove DEMO_MODE cleanly (no execution yet):

## Goal
Remove all DEMO_MODE code paths + fixtures and make the project always use the real Supabase-backed data access. Update docs accordingly.

## Scope (what will be removed/changed)

### Code to remove
- `app/data/demo.py` (the `demo_mode_enabled()` toggle)
- `app/data/fixtures.py` (all demo_* fixtures)
- All `if demo_mode_enabled(): ...` branches in:
  - `app/data/matches.py`
  - `app/data/bets.py`
  - `app/data/users.py`
- Any imports of `app.data.demo` / `app.data.fixtures`
- Update `app/data/__init__.py` docstring to remove DEMO_MODE mention

### Docs to update
- Remove the “DEMO_MODE” section from `README.md` and any instructions referencing it.

### Verification
- Grep ensures no `DEMO_MODE`, `demo_mode_enabled`, or fixture imports remain in `app/**`.
- Import checks still pass:
  - `python -c "import streamlit_app"`
  - `python -c "import app.worker.worker"`

## Step-by-step task plan
1. **Inventory + safety check**
   - Confirm all references (already found in: `app/data/*.py` + `README.md`).
2. **Remove demo modules**
   - Delete `app/data/demo.py` and `app/data/fixtures.py`.
3. **Simplify DAL modules**
   - Edit `app/data/matches.py`, `app/data/bets.py`, `app/data/users.py`:
     - remove demo imports
     - remove demo branches
     - keep only Supabase logic
4. **Update `app/data/__init__.py`**
   - Remove DEMO_MODE references in docstring.
5. **Update README**
   - Remove DEMO_MODE section completely.
6. **Final QA**
   - `grep` for DEMO_MODE/demo_mode_enabled/fixtures
   - import checks for Streamlit + worker

If you confirm this plan, I’ll execute it immediately and leave you with only “real mode” instructions for running Streamlit against Supabase.

---
**Plan ready for review.**
You can discuss or refine this plan, then approve or reject it.
