---
plan_id: plan-48657ec9
created: 2026-06-26T09:34:51.401362800+00:00
request: "Continue with path A"
status: approved
---

Plan (Path A): show **only the winner crest** after `karne:` outside DEMO_MODE (no changes executed yet)

## Goal
- **DEMO_MODE**: keep using codes everywhere  
  `karne: ENG`
- **Non-demo**: show only the **winner crest** (no code), inline after `karne:`  
  `karne: <img ...>`  
Applies to:
1) **Edit mode**: penalty DIP selector options  
2) **View mode**: bet line + actual score line

Important constraint: **stored value should remain stable** for scoring:
- We keep saving DIP as `karne: ENG` / `karne: ARG` (code-based)
- Only the UI label changes (crest vs code)

## Files involved
- `app/utils/components.py`
- (import) `app.data.demo.demo_mode_enabled` (to reliably detect DEMO_MODE)

## Steps
1) **Inspect current penalty DIP flow**
   - In `render_match_row()` edit mode: penalty DIP options currently are strings `karne: {home_code}` / `karne: {away_code}`.
   - In view mode: bet DIP is displayed under bet; actual outcome label uses `karne: {winner_code}`.

2) **Add helpers**
   - `_is_demo_mode()` using `demo_mode_enabled()`
   - `_crest_img(src: str) -> str` returning `<img src='...' width='16'>`
   - `_format_penalty_label(code, crest_url)`:
     - demo → `karne: CODE`
     - non-demo → `karne: <img ...>` (winner crest only)

3) **Edit mode selectbox: keep values as codes, display crests**
   - Keep `options` as stored DIP strings: `["karne: ENG", "karne: ARG"]`
   - Use `format_func=` in `st.selectbox()` to render:
     - demo: the same string
     - non-demo: `karne: <winner crest>` for that option
   - Result: DB still stores `karne: CODE`, scoring logic unchanged.

4) **View mode: display crest for penalty-type labels**
   - For the bet DIP under bet score:
     - if dip starts with `karne:`:
       - demo → show `karne: CODE`
       - non-demo → show `karne:` + crest for that code (home or away crest)
   - For actual scoreline label (penalty outcome):
     - demo → `karne: CODE`
     - non-demo → `karne:` + winner crest (derived from pens winner)

5) **Verification**
   - `python -m compileall -q .`
   - DEMO_MODE smoke: confirm still prints `karne: ENG`
   - Non-demo smoke (without running Streamlit): call helper with a sample record containing crests and ensure HTML is generated.

## Expected outcome
- DEMO_MODE UI shows text `karne: ENG`
- Normal UI shows `karne:` followed by the winner crest only, in both:
  - bet line (`… | karne: [crest]`)
  - actual score line (`(… | karne: [crest])`)
- Stored DIP values remain code-based (`karne: ENG`), so scoring stays stable.

If this plan matches, I’ll implement it next.

---
**Plan ready for review.**
You can discuss or refine this plan, then approve or reject it.
